import { Overview } from "@/components/Overview";

export const metadata = {
  title: "Overview · XOps Operational Graph",
  description: "What is broken, who it hits, and who has to answer.",
};

export default function OverviewPage() {
  return <Overview />;
}
